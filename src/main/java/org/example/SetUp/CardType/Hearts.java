package org.example.SetUp.CardType;

import org.example.SetUp.Cards;
import org.example.SetUp.Rank;
import org.example.SetUp.Suit;

public class Hearts extends Cards {
    public Hearts(Suit suit, Rank rank) {
        super(suit, rank);
    }
}
